# Demo 说明：风险与人工介入评估器（v1.4.0）

## 一、录屏文件（可公开访问/上传飞书）

- `Demo录屏_风险人工介入评估器.gif`：10 帧动画录屏，浏览器直接打开即可播放，可上传飞书云文档或任意平台
- 单帧截图：`screenshots/`（10 张 PNG）

## 二、录屏内容（正常流程 + 失败/边界场景，含 v1.4.0 回答边界四类）

| 帧 | 场景 | 展示结果 |
|----|------|---------|
| 1 | 首页（正常流程预设已载入） | 输入表单 + 快速场景 + 发货知识库（SP-01~SP-05） |
| 2 | 人工介入：发货时效承诺（48小时内发货） | HIGH · 必须人工介入（R10，无出处→政策出处缺失） |
| 3 | 人工介入：时效承诺 + 政策出处（SP-01） | HIGH · 可追溯 true，出处回显供人工核验（R13） |
| 4 | 人工介入：特殊尺寸计费 | HIGH · 必须人工介入（R11，知识库命中 SP-04） |
| 5 | 人工介入：价格例外/议价（内部价） | HIGH · 必须人工介入（R12，知识库命中 SP-05） |
| 6 | 边界2：缺规格 → 先澄清 | MEDIUM · 建议人工复核（R14，response_mode=clarify） |
| 7 | 边界3：部分回答（敏感肌已答，油皮未覆盖） | LOW · 建议人工复核（R15，response_mode=partial，声明未覆盖+下一步） |
| 8 | 边界4：超出业务范围（修手机） | LOW · 拒绝并说明边界（R16，response_mode=refuse_or_escalate） |
| 9 | 失败/边界：超长输入 2600 字 | ERROR · 2000 字边界拦截 |
| 10 | 失败/边界：置信度类型错误（abc） | ERROR · 类型校验不崩溃 |

## 三、本地可运行 Demo

```powershell
# 需要 Python 3.10+（纯标准库，无第三方依赖）
python server.py
# 浏览器打开 http://127.0.0.1:8765
```

- **纯静态部署（GitHub Pages）**：无需后端。页面在 `POST /api/evaluate` 不可用时，自动降级调用 `evaluator.js` 内置评估器（`evaluateRisk`），结果与 Python 版一致。

页面支持：
- 手动输入 query / answer / 品类 / 置信度 / 政策出处（policy_source）/ 风险标记后点「开始评估」
- 右侧「快速场景」按钮一键载入 15 个演示预设（含 6 个 v1.3.0 发货/出处场景 + 3 个 v1.4.0 回答边界场景）
- 「③ 发货知识库」面板通过 `GET /api/kb` 查询 SP-01~SP-05 政策条目，评估时自动高亮命中当前问题的条目
- 评估结果实时显示：风险等级徽标（绿=自动发送 / 橙=建议复核 / 红=必须人工 / 灰=输入无效）、人工介入要求、政策出处与可追溯性、回答边界四类（response_mode + 判定依据）、风险因素与决策理由

## 四、与真实评估器的关系

Demo 页面通过 `POST /api/evaluate` 调用 Skill 包内真实 Python 实现
（`../risk-human-intervention-skill/scripts/risk_evaluator.py`，v1.4.0），
非演示镜像，结果与评测集（68 条）一致。知识库内容与 `references/shipping-policy.md` 对应。

GitHub Pages 纯静态部署时，`evaluator.js` 为 Python 实现的等价前端移植（68 条评测逐字段一致），接口不可用时自动降级，Demo 可在无后端环境直接运行。
